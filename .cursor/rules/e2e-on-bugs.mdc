---
description: 新发现问题优先补 e2e 回归；修 bug 时同步加测
alwaysApply: true
---

# 新问题必须尽量补 e2e

用户报告或排查中**新发现**的缺陷（登录失败、场景错乱、按钮无响应、密码被改等），在修复时必须同步加回归，**优先 e2e**，不要只改代码不留防护。

## 强制步骤

1. **先复现并钉死断言**：用失败现象写成可重复检查（标题/文案/事件/重登结果等）。
2. **能走用户路径就写 e2e**：
   - 登录 / 新手链 / 房间 / 跟随 / 挂名 / 重登 → 优先 `deploy/scripts/e2e-login-smoke.cjs`（部署门禁）和/或 `web/app/e2e/`（Playwright）。
   - 纯解析、协议合并、FSM → 同步补单元测试；若同一问题用户也会踩到 UI/登录路径，**e2e 仍要补**。
3. **门禁优先**：影响进游戏或新手主路径的，必须让 `run-e2e-smoke.sh` 覆盖到；勿只加本地手工步骤。
4. **修完再跑**：相关 e2e / 单元测跑绿；环境不具备时说明跳过原因，并留下已写好的用例。

## 反例

- 只修 LPC/网关/前端，不写任何会失败的测试。
- 只用临时脚本验证一次，不并入 `e2e-login-smoke` 或 `web/app/e2e`。
- 把本可自动化的用户路径推给「以后再测」。

细则与命令见 `.cursor/skills/xkx-testing/SKILL.md`。
